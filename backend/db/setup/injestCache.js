/*! 
 * Needed to backburner injestCache.js because this will likely need
 * a great deal of error correct and capture to make sure the schemas
 * match before trying to merge.
*/
export const injestCache = (table) => {};
